PORT=5000
NODE_ENV=development
MONGODB_URI=
JWT_SECRET=dev-secret

# OTP settings
# Use "mock" for local/dev testing with a fixed OTP (default is 123456).
# Switch to "sms" when SMS gateway integration is added in src/services/otp.service.js.
OTP_MODE=mock
FAKE_OTP=123456
